📸 FamilyBook (v1.3.1)
FamilyBook — приватная социальная экосистема для семеи. Это PWA-приложение, сочетающее функции фотоархива, защищенной ленты событий и интерактивной площадки с механикой «постов-подарков».

📂 Структура проекта (Project Blueprint)
Plaintext
FAMILY_BOOK/
├── app/
│   ├── api/                # Логика эндпоинтов (auth.py, posts.py)
│   ├── static/             # Статические ресурсы
│   │   ├── css/            # Tailwind и кастомные стили
│   │   ├── icons/          # Иконки для PWA (192x192, 512x512)
│   │   ├── sounds/         # Звуки уведомлений (notification.mp3)
│   │   ├── uploads/        # Хранение фото и аватарок (UUID.webp)
│   │   ├── manifest.json   # Конфигурация PWA
│   │   ├── sw.js           # Service Worker для офлайн-режима
│   │   └── default_avatar.png # Заглушка для новых пользователей
│   ├── templates/          # Jinja2 шаблоны
│   │   ├── includes/       # Компоненты (navbar, post_card)
│   │   └── ...             # index.html, profile.html, post_detail.html
│   ├── config.py           # Управление средами (Dev/Test/Prod)
│   ├── database.py         # Подключение к SQLite (Engine)
│   ├── logger.py           # Аудит действий (Action Logging)
│   ├── main.py             # Точка входа FastAPI
│   ├── models.py           # SQLModel: User, Post, PostImage, Comment, PostLike
│   └── security.py         # Bcrypt и работа с сессиями
├── logs/                   # Журналы работы сервера
├── .env.dev                # Среда разработки
├── .env.test               # Среда для портфолио/тестов
├── .env.prod               # Секретные настройки для семьи
├── family_book.db          # База данных (SQLite)
└── README.md               # Технический паспорт проекта
🚀 Технический ландшафт
Backend: Python 3.13 + FastAPI. Использование асинхронности для обработки запросов.

Data Layer: SQLModel (SQLAlchemy 2.0). Сложные связи: User ↔ Post (1:N), Post ↔ PostImage (1:N), Post ↔ Comment (1:N). Лайки реализованы через Many-to-Many.

Media Engine: Автоматическая конвертация всех изображений в WebP (Pillow) для экстремального сжатия без потери качества.

PWA Ready: Установка на экран смартфона, работа без адресной строки, кастомная иконка и заставка.

✅ Реализованный функционал (v1.3.1)
Social Core: Полноценная система лайков с визуальными эффектами (canvas-confetti) и древовидные комментарии.

Gift Logic: Механика «распаковки» подарка в post_detail.html с автоматическим запуском салюта.

UI/UX:

Dark/Light темы с сохранением выбора в localStorage.

Система Flash-уведомлений со звуковым сопровождением.

Адаптивная верстка под мобильные устройства.

Security: Сессионная авторизация на базе Cookies с защитой подписи ключом из .env.

⚙️ Установка и запуск (Test Mode)
Клонируйте репозиторий:

Bash
git clone https://github.com/mikhail/family_book.git
Запустите в тестовом режиме:

Bash
set APP_MODE=test && poetry run uvicorn app.main:app --reload
Разработчик: Михаил (2026)